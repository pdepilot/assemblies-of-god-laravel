<x-app-layout><x-slot name="header"><h2 class="font-semibold text-xl">Notifications</h2></x-slot><div class="py-10"><div class="max-w-4xl mx-auto sm:px-6 lg:px-8 space-y-4">@include("communication-hub._nav", ["canManage" => $canManage])@forelse($items as $item)<div class="bg-white shadow-sm sm:rounded-lg p-4 flex justify-between"><span>{{ $item["title"] }}</span>@if(!$item["is_read"])<form method="POST" action="{{ route("communication-hub.notifications.read", $item["id"]) }}">@csrf<button class="text-indigo-600 text-sm">Mark read</button></form>@endif</div>@empty<p>No notifications.</p>@endforelse</div></div></x-app-layout>
